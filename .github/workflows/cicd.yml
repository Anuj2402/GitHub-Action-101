#This is the name of the work flow that will be displayed in the action tab 
name: CICD

#manual click (deliver workflow ) Whenever user needs 
on: 
  workflow_dispatch:

jobs:

    code:
       runs-on: ubuntu-latest
       steps:
         - name: checkout code 
           run: echo "Code clone from git hub"
           
    build:
       runs-on: ubuntu-latest
       steps:
          - name: Build Docekr Container  
            run: echo "Docker container build"
    test:
        runs-on: ubuntu-latest
        steps:
          - name: Test the container 
            run: echo "testing the container"
    deploy: 
        runs-on: ubuntu-latest
        steps:
          - name: deploying the app
            run: echo "Docker compose deployed the app"
    

 
